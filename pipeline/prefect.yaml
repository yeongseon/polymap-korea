name: polymap-korea-pipeline
prefect-version: 3.1.0

build: []
push: []
pull: []

deployments:
  - name: curated-publish
    version: 0.1.0
    description: Write curated JSON snapshots and, when API_URL is configured, post them to the API for database indexing. OpenSearch wiring is planned but not active yet.
    entrypoint: flows/publish.py:publish_snapshot
    work_pool:
      name: polymap-pool
