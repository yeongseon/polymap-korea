/**
 * Auto-generated OpenAPI types for PolyMap Korea API.
 *
 * Regenerate by running: apps/web/scripts/generate-types.sh
 * Requires the API server to be running locally.
 *
 * These types are the source of truth for frontend/backend contract alignment.
 * When the API schema changes, regenerate and fix any type errors in the web app.
 */

// Placeholder — run generate-types.sh with a live API server to populate.
export type paths = Record<string, never>;

export interface components {
  schemas: Record<string, never>;
}
